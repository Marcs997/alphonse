/* config.js — réglage partagé par le jeu (track.js) et la page admin (admin.js).
   Colle ici l'URL "/exec" de TON déploiement Apps Script (voir README → Admin).
   Cette URL n'est pas secrète (le mot de passe, lui, reste côté Google). */

window.ALPH_SCRIPT_URL = "REMPLACER_PAR_TON_URL_EXEC";
